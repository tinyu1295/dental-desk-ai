data "aws_availability_zones" "available" {
  state = "available"
}

resource "aws_vpc" "main" {
  cidr_block           = "10.0.0.0/16"
  enable_dns_support   = true
  enable_dns_hostnames = true

  tags = {
    Name = "${var.project_name}-vpc"
  }
}

resource "aws_internet_gateway" "main" {
  vpc_id = aws_vpc.main.id

  tags = {
    Name = "${var.project_name}-igw"
  }
}

# 2 public subnets across 2 AZs, for the single EC2 host. No NAT Gateway -
# the host is its own internet-facing front door (nginx on 80/443), and
# nothing else in this VPC needs outbound internet access, so there's
# nothing a NAT Gateway would be for. Security groups, not subnet
# placement, are what actually restrict access to the containers.
resource "aws_subnet" "public" {
  count                   = 2
  vpc_id                  = aws_vpc.main.id
  cidr_block              = "10.0.${count.index}.0/24"
  availability_zone       = data.aws_availability_zones.available.names[count.index]
  map_public_ip_on_launch = true

  tags = {
    Name = "${var.project_name}-public-${count.index}"
  }
}

resource "aws_route_table" "public" {
  vpc_id = aws_vpc.main.id

  route {
    cidr_block = "0.0.0.0/0"
    gateway_id = aws_internet_gateway.main.id
  }

  tags = {
    Name = "${var.project_name}-public-rt"
  }
}

resource "aws_route_table_association" "public" {
  count          = 2
  subnet_id      = aws_subnet.public[count.index].id
  route_table_id = aws_route_table.public.id
}

# 2 private subnets across 2 AZs, for RDS only - a DB subnet group needs
# subnets in at least 2 AZs even for a single, non-Multi-AZ instance.
# "Private" here just means no route to the Internet Gateway; RDS never
# initiates outbound internet traffic on its own, so unlike a NAT Gateway
# for a service that needs to call out, there's no ongoing cost to giving
# it a real private subnet instead of reusing the public ones.
resource "aws_subnet" "private" {
  count             = 2
  vpc_id            = aws_vpc.main.id
  cidr_block        = "10.0.${count.index + 10}.0/24"
  availability_zone = data.aws_availability_zones.available.names[count.index]

  tags = {
    Name = "${var.project_name}-private-${count.index}"
  }
}

# Deliberately no routes added beyond the implicit local one - this is
# what makes the subnets above actually private, rather than just
# unlabelled public subnets that happen to host RDS.
resource "aws_route_table" "private" {
  vpc_id = aws_vpc.main.id

  tags = {
    Name = "${var.project_name}-private-rt"
  }
}

resource "aws_route_table_association" "private" {
  count          = 2
  subnet_id      = aws_subnet.private[count.index].id
  route_table_id = aws_route_table.private.id
}
